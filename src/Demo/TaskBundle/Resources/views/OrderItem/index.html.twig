{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OrderItem list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Amount</th>
                <th>Tax</th>
                <th>Quantity</th>
                <th>Total</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('demo_taskorderitem_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.amount }}</td>
                <td>{{ entity.tax }}</td>
                <td>{{ entity.quantity }}</td>
                <td>{{ entity.total }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('demo_taskorderitem_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('demo_taskorderitem_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('demo_taskorderitem_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
