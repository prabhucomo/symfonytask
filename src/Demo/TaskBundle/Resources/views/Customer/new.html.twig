{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Customer creation</h1>

    <form action="{{ path('demo_taskcustomer_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('demo_taskcustomer') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
